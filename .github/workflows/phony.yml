name: Phony workflow to test out GA workflow layout

on:
 workflow_dispatch:




jobs:
  checkout:
    name: Checkout Code
    runs-on: ubuntu-latest
    steps:
      - name: 1
      - run: echo "Weeeee!"
  
  wget_repo_db:
    name: get files
    runs-on: ubuntu-latest
    needs: checkout
    steps:
      - run: echo "2"

  build:
    name: build
    runs-on: ubuntu-latest
    needs: wget_repo_db
    steps:
      - run: echo "3"

  wget_again:
    name: wget2
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "4"

  build_again:
    name: build again
    runs-on: ubuntu-latest
    needs: wget_again
    steps:
      - run: echo "5"


 